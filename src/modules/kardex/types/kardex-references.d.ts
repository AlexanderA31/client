export interface I_ProductReference {
    id: string;
    name: string;
    code?: string;
}

export interface I_WarehouseReference {
    id: string;
    name: string;
}

export interface I_UserReference {
    id: string;
    name: string;
}
